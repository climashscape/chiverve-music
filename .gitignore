# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
node_modules.bak*/
jspm_packages/

# TypeScript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# next.js build output
.next


build

dist

# 开发版（npm run dev）的产物：主进程与 user-api-preload，与 dist/ 分开以免互相撕
dist-dev

src/**/*-internal.js

# Security scan hook runtime state (Mimosa)
.mimosa/

# ZCode 会话工具状态（plan 产物与 spec 重复，不入库）
.zcode/plans/

# 本地工单与探索笔记：只留本机，不入公开历史（2026-09-24 用户定；历史已用 git filter 重写去掉）
.scratch/

# 验收脚本与留档产物：放**仓库目录内**（跟着仓库走，不像 /tmp 那样会丢），但**不入公开树**
# （2026-09-25 用户定：「收进仓库但是要加入忽略」）。约定见 scripts/verify/README.md
scripts/verify/

# 编辑器 / 操作系统杂项（公开仓库的第一个 macOS 贡献者最容易带上 .DS_Store）
.DS_Store
Thumbs.db
Desktop.ini
.idea/
*.swp
*~

# 内部文档与 agent 工作区：**只留本机，不进公开树**（2026-09-24 用户定：本仓库只公开源码 + README + 许可）。
# 这几个路径仍留在工作区（本地工作流与 agent 照常读它们），只是不入库；
# 历史里的这些路径会在首次公开前用 git filter-repo 一并抹掉（见 .scratch/release-prep/issues/09）。
/AGENTS.md
/CONTEXT.md
/docs/
/.agents/
/.zcode/
# ZCode 的本地搜索忽略规则（由「从 .gitignore 同步」生成，内容含上面这些不入库路径），同属本机工具状态
/.zcodeignore
